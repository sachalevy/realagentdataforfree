[
    {
        "app_name": "Code",
        "time": "12s",
        "context": "def take_screenshot(file_path):Takes. a-screenshot-and saves it to the specified file path.Args:file_path (str): ~The path where the screenshot willbe saved.time.sleep(3)mouse_position = get_mouse_position( )try:subprocess.run(['screencapture', file_path], check=True)print(f'Screenshot saved to {file_path}')except subprocess.CalledProcessError ase:print(f'An error occurred while taking the screenshot: {e}')return mouse_positionscreenshot_dir = Path('/Users/sachalevy/IMPLEMENT/datamakr/data/')screenshot_dir.mkdir(parents=True, exist_ok=True)c= Ofilepath = screenshot_dir / f'screen_{c}.png'(variable) filepath: Pathmouse_position =-Nonemouse_position = take_screenshot(filepath)from omegaconf import OmegaConfif not mouse_position:mouse_position = OmegaConf.create({'x': 493, 'y': 576})else:mouse_position = OmegaConf.create({'x': mouse_position.x, 'y': mouse_position.y})print(mouse_position)mouse_position.x = mouse_position.x * 2mouse_position.y = mouse_position.y * 2"
    },
    {
        "app_name": "Safari",
        "time": "30s",
        "context": "LLaVA-vI.5-I3B 256 Ie-3 I 2O48 O2. FinetuningHyperparameter I Global Batch Size I Learningrate I Epochs I Max length I Weight decayLLaVA-vI.5-I3B I28 2e-5 I 2O48 ODownload Vicuna checkpoints (automatically) 2Our base model Vicuna vI.5, which is an instruction-tuned chatbot, will be downloaded automatically when you runour provided training scripts. No action is needed.Pretrain (feature alignment) 2Please download the 558K subset of the LAION-CC-SBU dataset with BLIP captions we use in the paper here.Pretrain takes around 5.5 hours for LLaVA-vI.5-I3B on 8x AIOO (8OG), due to the increased resolution to 336px. Ittakes around 3.5 hours for LLaVA-vI.5-7B.Training script with DeepSpeed ZeRO-2: pretrain.sh.e --mm_projector_type mlp2x_gelu : the two-layer MLP vision-language connector.e --vision_tower openai/clip-vit-large-patchI4-336 : CLIP ViT-L/I4 336px.> Pretrain takes around 2O hours for LLaVA-7B on 8x VIOO (32G)Visual Instruction Tuning 2I. Prepare dataPlease download the annotation of the final mixture our instruction tuning data Ilava_vI_5_mix665k.json, anddownload the images from constituting datasets: COCO: train2OI7 GQA: images OCR-VQA: download script, we save all files as .jpge TextVQA: train_val_imagese VisualGenome: partI, part2After downloading all of them, organize the data as follows in ./playground/data ,coco (ol"
    }
]
